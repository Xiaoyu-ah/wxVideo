# 实际生成与验证记录

日期：2026-09-20。记录对应本项目的演示片，不代表已经完整审阅历史作品。

## 成片

- 内容：看到AI新功能，先问三句话；六场原创方法演示。
- 时间线：70.719958秒；MP4容器约70.742秒（编码帧与音频封装取整）。
- 画面：1080×1920，30fps，H.264。
- 声音：六段Windows中文系统合成音；导出为AAC、48kHz、双声道。
- 字幕：19条，按逐句WAV实际采样时长对齐；没有使用纯文字长度估算。
- 封面：1080×1440 PNG；头像：新生成的正方形PNG提案。

## 已执行的检查

1. Hyperframes 0.8.55完整check：lint、runtime、layout均0错误0警告；79项文字对比度检查全部通过。两条layout信息是转场时相同位置的账号名交叠，属于淡化衔接，不是不同文案遮挡。
2. 检查抽帧中的中文标题、图卡、字幕与来源；实际MP4抽帧检查中发现对比卡片孤字换行，已加入均衡换行并重新生成成片。
3. FFprobe核对H.264/AAC、画幅、帧率和时长。FFmpeg音量检测确认有实际音频信号，峰值约-1.5dB；这不能代替听觉质量审阅。
4. Windows一键入口已执行；Python自动检测、复用配音、HTML生成、完整check及导出流程可以运行。
5. 日期脚本验证：合法日期生成六场60秒待填稿；拒绝非法日期，拒绝覆盖同日文件；生成JSON可以通过视频构建器验证。
6. 封面PNG已实际查看，未见文字裁切。

## 没有完成或不能推出的结论

- 历史视频完整视听审阅数为0；历史原声、音乐和精确剪辑节奏未知。
- 未宣称对样片做过人工完整试听，发布前仍应从头播放确认语音读音与感受。
- check的独立motion审计未启用。附带animation-map脚本因npm环境缺少开发包`@hyperframes/producer`在启动前失败，不能标为编舞审计通过；已完成的画面抽检与渲染验证独立有效。
- 未验证新头像名称在微信实际保存，未自动发布视频。

## 复现

在项目目录运行 `powershell -File scripts/make-video.ps1 -ReuseAudio`，使用随附配音重新检查并导出。发布新内容需要核实新来源、改稿、重新合成并查看新成片。
